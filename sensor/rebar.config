{deps, [
    grisp
]}.

{erl_opts, [debug_info]}.

{plugins, [rebar3_grisp]}.

{grisp, [
    {otp, [
        {version, "25"}
    ]},
    {deploy, [
        {destination, "/media/daube/GRISP"}
    ]}
]}.

{shell, [{apps, []}]}.

{relx, [
    {release, {sensor, "0.1.0"}, [
        sensor
    ]}
]}.
